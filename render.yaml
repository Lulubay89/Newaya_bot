services:
  - type: web
    plan: free
    name: newaya_bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TOKEN
        sync: false
